#include <algorithm>
#include <iostream>
#include <queue>
#include <vector>

using namespace std;

// 这个要计算出能够有多少条线可用，并且要知道最少需要多少可以达到联通，两者比较即可
class Solution {
public:
    int makeConnected(int n, vector<vector<int>>& connections)
    {
    }
};

int main(int argc, char* argv[])
{
    Solution so;
    return 0;
}