#include <algorithm>
#include <iostream>
#include <vector>

using namespace std;

// 6.4
class Solution {
public:
    int lastStoneWeight(vector<int>& stones)
    {
        // make a max-heap from stones
    }
};

int main(int argc, char* argv[])
{
    Solution so;

    return 0;
}