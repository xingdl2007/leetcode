#include <algorithm>
#include <iostream>
#include <queue>
#include <vector>

using namespace std;

// 类似田忌赛马
class Solution {
 public:
  vector<int> advantageCount(vector<int>& A, vector<int>& B) {}
};

int main(int argc, char* argv[]) {
  Solution so;
  return 0;
}